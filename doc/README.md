# API Docs

`overview.md` 是 Dokka 聚合文档首页，用来说明 API 的推荐写法和参数含义。

生成后的 API 文档位于 `doc/api/index.html`。

重新生成命令：

```bash
./gradlew generateApiDocs
```

当前聚合模块：

- `core`
- `hook-xposed-82`
- `hook-xposed-101`
